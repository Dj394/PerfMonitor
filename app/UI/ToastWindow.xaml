<Window x:Class="PerfMonitorLive.UI.ToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PerfMonitor" Width="380" SizeToContent="Height" MinHeight="100"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" ShowInTaskbar="False" ShowActivated="False" Topmost="True"
        WindowStartupLocation="Manual" Focusable="False" FontFamily="Segoe UI">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBgBrush}"/><Setter Property="Foreground" Value="{DynamicResource FgBrush}"/>
            <Setter Property="Padding" Value="10,4"/><Setter Property="Cursor" Value="Hand"/><Setter Property="FontSize" Value="12"/><Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="7" Padding="{TemplateBinding Padding}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                        <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Background" Value="{DynamicResource ButtonHoverBrush}"/></Trigger></ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border x:Name="Card" CornerRadius="14" Background="{DynamicResource ToastBgBrush}" BorderBrush="{DynamicResource ToastBorderBrush}" BorderThickness="1" Margin="4" Cursor="Hand">
        <Border.Effect><DropShadowEffect BlurRadius="14" ShadowDepth="0" Opacity="0.55"/></Border.Effect>
        <Grid Margin="12,10,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Border x:Name="Dot" Width="38" Height="38" CornerRadius="19" Background="#33F5B73D" VerticalAlignment="Top" Margin="0,0,12,0"><TextBlock x:Name="IconText" Text="⚠️" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock x:Name="TitleText" Foreground="{DynamicResource TitleFgBrush}" FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap"/>
                <TextBlock x:Name="DetailText" Foreground="{DynamicResource MutedBrush}" FontSize="12" TextWrapping="Wrap" Margin="0,3,0,0" MaxHeight="120"/>
                <WrapPanel x:Name="ActionsPanel" Margin="0,8,0,0" Visibility="Collapsed"/>
            </StackPanel>
            <Button x:Name="CloseBtn" Grid.Column="2" Content="✕" Foreground="{DynamicResource MutedBrush}" Background="Transparent" FontSize="12"
                    VerticalAlignment="Top" Padding="6,0" Cursor="Hand" Focusable="False"/>
        </Grid>
    </Border>
</Window>
